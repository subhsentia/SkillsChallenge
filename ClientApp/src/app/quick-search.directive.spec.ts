import { QuickSearchDirective } from './quick-search.directive';

describe('QuickSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new QuickSearchDirective();
    expect(directive).toBeTruthy();
  });
});
